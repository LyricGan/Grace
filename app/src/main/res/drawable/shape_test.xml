<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
     <?xml version="1.0" encoding="utf-8"?>
    <shape
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:shape=["rectangle" | "oval" | "line" | "ring"] >   - 默认为rectangle
        <corners  - shape=“rectangle”时使用，
            android:radius="integer"  - 半径，会被下边的属性覆盖，默认为1dp，
            android:topLeftRadius="integer"
            android:topRightRadius="integer"
            android:bottomLeftRadius="integer"
            android:bottomRightRadius="integer" />
        <gradient  - 渐变
            android:angle="integer"
            android:centerX="integer"
            android:centerY="integer"
            android:centerColor="integer"
            android:endColor="color"
            android:gradientRadius="integer"
            android:startColor="color"
            android:type=["linear" | "radial" | "sweep"]
            android:useLevel=["true" | "false"] />
            android:startColor和android:endColor分别为起始和结束颜色，
            android:angle是渐变角度，必须为45的整数倍。
            另外渐变默认的模式为android:type="linear"，即线性渐变，可以指定渐变为径向渐变，
            android:type="radial"，径向渐变需要指定半径android:gradientRadius="50"。
　　         android:angle，最原始的，即android:angle＝“0”时，是从左到右，按照开始颜色到结束颜色来渲染的，
            android:angle＝“90”是从下到上来渲染的，
            android:angle＝“270”是从上到下来渲染的，
            android:angle＝“180”是从右到左来渲染的，
            android:angle＝“360”和android:angle＝“0”是一样的，
            渲染时按照最原始的渲染色板围绕控件中心来逆时针旋转相应的度数，
            即android:angle里面的值就是所需要旋转的角度，只是这个旋转角度必须是45的整数倍
        <padding
            android:left="integer"
            android:top="integer"
            android:right="integer"
            android:bottom="integer" />
        <size    - 指定大小，一般用在ImageView配合scaleType属性使用。大小一般会适配滴
            android:width="integer"
            android:height="integer" />
        <solid    - 填充颜色，可是是十六进制颜色。（比如想设置半透明效果，直接使用十六就只就OK）
            android:color="color" />
        <stroke    - 指定边框，border，dashWidth和dashGap有一个为0dp则为实线
            android:width="integer"
            android:color="color"
            android:dashWidth="integer"    - 虚线宽度
            android:dashGap="integer" />    - 虚线间隔宽度
    </shape>

    颜色对照表：http://blog.sina.com.cn/s/blog_7ed4baf90100sbn6.html
     -->
</selector>